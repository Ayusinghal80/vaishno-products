# Vaishno Products V4

New: change logo, add multiple users, Admin/Editor roles, change your password, admin password reset, delete users, server-side authentication, product image uploads.

Run in PowerShell: `npm install` then `npm.cmd start`.
Website: http://localhost:3000
Admin: http://localhost:3000/admin.html
Default login: admin / change-this-password
CHANGE THE PASSWORD IMMEDIATELY.

For production, use HTTPS, environment variables and a persistent database/cloud storage.
